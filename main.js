const colors = ['red', 'orange', 'yellow', 'green', 'blue', 'indigo', 'violet'];

// let spans = document.querySelectorAll('span');
console.log(document)

let index = 0
for (each of spans) {
    each.style.color = colors[index];
    index += 1
};